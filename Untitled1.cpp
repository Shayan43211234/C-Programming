#include<stdio.h>
int main()
{
	int result, min, max, mid;
	maze (numAnUB) numanum numarhum): (num num) Num:num)
	min (numa num ?(numa num urs:nunc nuncnun)nuni nunc
	mid(numA+numA number)-(min max)
	result(max int mid);
	return result;
	return 0;
}
